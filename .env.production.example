# Production environment configuration template
# GitHub Actions creates .env.production from secrets during deployment

# Production API URL (same-origin routing with /api/v1 prefix)
VITE_API_URL=/api/v1
